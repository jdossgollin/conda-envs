# work with tensorflow and related packages
name: tensorflow
channels:
    - conda-forge
    - defaults
dependencies:
    - python=3.6        # use a recent version of python
    - tensorflow>=1.10  # recent tf version
    - matplotlib
    - seaborn
    - numpy
    - jupyter
    - pandas