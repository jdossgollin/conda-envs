# work with pytorch and related packages
name: pytorch
channels:
    - pytorch
    - defaults
dependencies:
    - python=3.6        # use a recent version of python
    - matplotlib
    - seaborn
    - numpy
    - jupyter
    - pandas
    - pytorch
    - torchvision